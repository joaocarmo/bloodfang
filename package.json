{
  "name": "bloodfang",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "typescript": "~5.7.0",
    "vitest": "^3.0.0",
    "tsup": "^8.0.0"
  }
}
